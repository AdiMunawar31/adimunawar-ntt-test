import { Typography } from "@mui/material";

// ==============================|| MAIN LOGO ||============================== //

export default function LogoSection() {
  return (
    <Typography variant="h4" sx={{ color: "MenuText", mt: 1 }}>
      ADI NTT TEST
    </Typography>
  );
}
